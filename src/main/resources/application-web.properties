# Web mode profile - Disable batch job and run as web server
spring.batch.job.enabled=false

# Enable web server (override batch mode default)
spring.main.web-application-type=servlet

# Skip checksum cache preloading (not needed for restore GUI)
heimdall.skip-checksum-cache=true
